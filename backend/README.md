# Backend (Django + DRF)

This is the backend starter scaffold for the Inclusive Document Reader project.

- `accounts` app: registration and login APIs
- `documents` app: upload, OCR placeholder, list, detail, and search APIs
